Clique com o botão direito do mouse dentro da pasta que queira criar o repositório git -> depois clique em "Git Bash Here" -> irá aparecer a linha de comando do git

git init -> cria o repositório git

git add <"nome do arquivo" ou --all> -> envia o arquivo para stage area

git commit -m  "mensagem"-> confirma as alterações e inclui uma mensagem obs.: se for somente uma palavra não precisa de aspas

git remote add origin <link do repositório do github>

git config --global user.name "<seu nome>" -> Nome que será exibido no github

git config --global user.email "<o email cadastrado no github>" -> email cadastrado github para acesso

Username for 'https://github.com': -> colocar o login criado no github
Password for 'https://github.com': -> colocar a mesma senha do github

git push -u origin master

OBS: Pode ser apareça a tela de login. Neste caso, basta informar os dados de login do github

-------- Proxy ------------------
git config --global http.proxy http://proxyUsername:proxyPassword@proxy.server.com:port

------- Resolver Erro fatal: refusing to merge unrelated histories ------------------
Link
https://stackoverflow.com/questions/37937984/git-refusing-to-merge-unrelated-histories-on-rebase

Solução
git pull origin master --allow-unrelated-histories





